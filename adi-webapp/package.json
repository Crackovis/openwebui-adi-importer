{
  "name": "adi-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root for ADI Importer (server + web)",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently --names \"server,web\" --prefix-colors \"cyan,magenta\" \"npm run dev --workspace=server\" \"npm run dev --workspace=web\"",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm run test --workspace=server && npm run test --workspace=web",
    "clean": "rimraf node_modules server/node_modules web/node_modules server/dist web/dist",
    "rebuild": "npm run clean && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.10"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=8.0.0"
  }
}
